import { LimitMoviesPipe } from './limit-movies.pipe';

describe('LimitMoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitMoviesPipe();
    expect(pipe).toBeTruthy();
  });
});

import { HighlightTitleDirective } from './highlight-title.directive';

describe('HighlightTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTitleDirective();
    expect(directive).toBeTruthy();
  });
});
